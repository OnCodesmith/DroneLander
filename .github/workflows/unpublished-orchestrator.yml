name: Unpublished Orchestrator
run-name: Unpublished Orchestrator (${{ github.event.action }}) 

on:
  release:
    types: [unpublished]

jobs:
  other-job:
    name: Other    
    runs-on: ubuntu-latest
    steps:
    - run: | 
        echo '${{ github.event_name }}' 
        echo '${{ github.event.action }}' 
    
    