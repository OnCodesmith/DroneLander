name: Created Orchestrator
run-name: Created Orchestrator '${{ github.event.action }}' 

on:
  release:
    types: [created]

jobs:
  
  other-job:
    name: Other    
    runs-on: ubuntu-latest
    steps:
    - run: | 
        echo '${{ github.event_name }}' 
        echo '${{ github.event.action }}' 
    
    